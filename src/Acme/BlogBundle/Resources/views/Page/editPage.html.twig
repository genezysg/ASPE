{% block content %}
    <h1>Edit Page Form</h1>
    {%  if (form is not null) %}
    <form action="{{ url('api_1_put_page', {'id': '1'}) }}" method="POST" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <input type="submit" value="submit">
    </form>
    <input type="submit" value="delete" >
    {% endif %}
{% endblock %}