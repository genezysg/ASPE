{% block content %}
<script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.3.10/angular.min.js">
$go = function() {
	$http.put("{{ url('api_1_put_page', {'id': id}) }}")
};
	
}
</script>
    <h1>Edit Page Form</h1>
    {%  if (form is not null) %}
    <form action="{{ url('api_1_post_page', {'id': id}) }}" method="POST" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <input type="submit" value="submit" ng-click="go()">
    </form>
    {% endif %}
{% endblock %}