{% block content %}
    <h1>Registro de Dados do Ementa</h1>
    {%  if (form is not null) %}
    <form action="{{ url('aspe_post_ementa') }}" method="POST" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <input type="submit" value="submit">
    </form>
    {% endif %}
{% endblock %}